
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Year in School</title>
</head>
<body>
    <p>You started school in <span id="start-year"></span> and you are currently in your <span id="current-year"></span> year.</p>
    <script>
        // Define the year you started school
        const startYear = 2022;
        
        // Get the current year
        const currentYear = new Date().getFullYear();
        
        // Calculate the school year
        let schoolYear = currentYear - startYear + 1;

        // Display the year you started school
        document.querySelector('#start-year').textContent = String(startYear);

        // Check if the calculated school year is as per your claim of being in the second year
        // If it's correct, display it
        if (schoolYear === 2) {
            document.querySelector('#current-year').textContent = schoolYear + "nd";
        } else {
            // Handle error or discrepancy in the claimed school year
            console.error("The calculated school year is not matching the claim of being in the 2nd year.");
            document.querySelector('#current-year').textContent = "N/A";
        }
    </script>
</body>
</html>